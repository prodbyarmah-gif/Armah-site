/**
 * Compatibility export only. The homepage relationship presentation is derived
 * from the locked Career Map data; no booking data is maintained here.
 */
export { homepageRelationships as repeatBookingProof } from '../lib/repeatBookings';
export type { HomepageRelationship as RepeatBookingProof } from '../lib/repeatBookings';
